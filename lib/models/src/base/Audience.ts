export enum Displays {
  SPONSOR = 0,
  MATCH_PREVIEW = 1,
  MATCH_START = 2,
  MATCH_RESULTS = 3,
  RANKINGS = 4,
  BLANK = 5
}

export enum DisplayModes {
  DEFAULT = 'default',
  TIMER_ONLY = 'timer-only',
  STREAM = 'stream'
}
