import { FC } from 'react';
import { useSocket } from 'src/api/use-socket.js';

export const Settings: FC = () => {
  const [socket] = useSocket();

  // jotai is the future!

  return <div>ur mom</div>;
};
