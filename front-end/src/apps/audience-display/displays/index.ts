import { FC } from 'react';

interface Props {
  id: number;
  eventKey: string | null | undefined;
  mode?: string;
}

const Displays: FC<Props> = () => {
  return null;
};

export default Displays;
