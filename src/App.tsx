import { Button } from '@mui/material';
import './App.less';

function App() {
  return (
    <div>
      <Button color='primary' variant='contained'>
        Primary Button
      </Button>
    </div>
  );
}

export default App;
